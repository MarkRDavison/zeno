language: cpp
dist: bionic
os: linux

jobs:
    -   BUILD_TYPE=Debug
    include:
        -   os: windows
  
#    - os: linux
#      dist: bionic
#      addons:
#          apt:
#              sources:
#                - ubuntu-toolchain-r-test
#              packages:
#                - g++-7
#
#    - os: linux
#      dist: bionic
#      addons:
#          apt:
#              sources:
#                - ubuntu-toolchain-r-test
#                - llvm-toolchain-bionic-7
#              packages:
#                - clang-7
#
#    - os: osx
#      osx_image: xcode10
#      compiler: clang
#      env: 
#        - COMPILER=clang++
#        - CPP17=1




install:
    -   git submodule update --init --recursive
    -   cmake --version
    -   $CXX --version
    -   ${BUILD_TYPE}

script:
    -   echo $BUILD_TYPE
    -   mkdir build
    -   cd build
    -   cmake --config $BUILD_TYPE ..
    -   cmake --config $BUILD_TYPE --build .
    -   ctest --build-config $BUILD_TYPE --no-label-summary --no-subproject-summary --output-on-failure